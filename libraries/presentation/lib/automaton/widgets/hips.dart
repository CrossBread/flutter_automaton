import 'package:flutter/material.dart';

class Hips extends StatelessWidget {
  const Hips({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
