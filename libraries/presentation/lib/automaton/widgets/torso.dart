import 'package:flutter/material.dart';

class Torso extends StatelessWidget {
  const Torso({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
