import 'package:flutter/material.dart';

class StatusConnector extends StatelessWidget {
  const StatusConnector({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
