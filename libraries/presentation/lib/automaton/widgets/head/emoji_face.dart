import 'package:flutter/material.dart';

class EmojiFace extends StatelessWidget {
  const EmojiFace({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
